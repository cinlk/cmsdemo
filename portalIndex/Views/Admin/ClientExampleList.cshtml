
@model IEnumerable<dynamic>


<h1>@RenderTitleSegments(T["Client Example List"])</h1>

@if (Model.Any())
{
    <ul class="list-group">
        @foreach (var contentItemSummary in Model)
        {
            <li class="list-group-item">
                @await DisplayAsync(contentItemSummary)
            </li>
        }
    </ul>
}
else
{
    <div class="alert alert-info">
        @T["No result found."]
    </div>
}